# Pinned to the exact versions the project was developed and tested against,
# so CI / a fresh clone / the graders get the same behaviour as we do.
# (SQLAlchemy 2.1.0 broke UUID handling on the SQLite test backend the day it
# was released — floating ">=" ranges are not worth the roulette.)
fastapi==0.136.3
uvicorn[standard]==0.48.0
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
alembic==1.18.4
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0  # used directly by core/security.py (previously pulled in via passlib[bcrypt])
python-multipart==0.0.29
websockets==16.0
pytest==9.0.3
pytest-asyncio==1.4.0
httpx==0.28.1
aiosqlite==0.22.1  # in-memory DB for tests/test_integration_rest.py
ruff==0.15.15  # linter (config in pyproject.toml); CI runs `ruff check`
